{
  "name": "georumble",
  "private": true,
  "scripts": {
    "dev": "docker compose up --build",
    "install:all": "npm --prefix client install && npm --prefix worker install",
    "packs:validate": "node scripts/validate-mappack.mjs assets/mappacks/*.mappack.json",
    "packs:build": "node scripts/fetch-mappacks.mjs && node scripts/build-europe.mjs && node scripts/build-asia.mjs && node scripts/build-south-america.mjs && node scripts/build-africa.mjs && npm run packs:validate"
  }
}
