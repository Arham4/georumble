{
  "name": "georumble",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "docker compose up --build",
    "install:all": "npm --prefix client install && npm --prefix worker install",
    "test": "node --test tests/*.test.mjs tests/*.test.ts",
    "packs:validate": "node scripts/validate-mappack.mjs assets/mappacks/*.mappack.json",
    "deploy": "npm --prefix client run build && npx --prefix worker wrangler deploy --config worker/wrangler.jsonc && node scripts/post-patch-notes.mjs",
    "packs:build": "node scripts/run-packs-build.mjs"
  }
}
